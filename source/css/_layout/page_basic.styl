.main_column#page
.main_column#post
  if hexo-config('custom.block-style') == 'soft'
    box-shadow $soft_block_style_shadow_main_column
    background $soft_block_style_bg
  else
    background-color var(--global-background-color)
    box-shadow 0px 0px 1rem var(--shadow-color)
  if hexo-config('custom.global-blur') != 0
    backdrop-filter: saturate(180%) blur($global-blur)
  border-radius $border-radius
  margin-top $block-margin
  transition all 0.5s
.main_column#page:hover
.main_column#post:hover
  if hexo-config('custom.block-style') == 'soft'
    box-shadow $soft_block_style_shadow_hover
  else
    box-shadow 0px 0px 1rem var(--shadow-color-deep)

.main_column#post article#post
.main_column#page article#page
  padding $block-padding